"""Stage 2 — extraction package (plan 02 / plan 03 own its modules).

The Foundation orchestrator imports stage-2 modules lazily and treats every
failure as a per-file/per-email ``unreadable`` outcome.
"""
