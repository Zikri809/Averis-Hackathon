# Participant app dependencies (plan 07 O1).
# The organizers' image already ships fastapi + uvicorn; these are the parser
# deps it omits, plus the test client used by tests/test_server.py.
fastapi>=0.110
uvicorn>=0.27
openpyxl>=3.1
python-docx>=1.1
pymupdf>=1.24
pytest>=8.0
httpx>=0.27
